# --- Frontend (Vite) env ---
# Dev-only: where the Vite dev server proxies /api requests (the backend).
# In production the built files are served by FastAPI, so this is ignored.
VITE_API_TARGET=http://127.0.0.1:8000
