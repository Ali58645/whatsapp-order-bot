# Railway build — Python API + React dashboard in one service.
providers = ["python", "node"]

[phases.build]
cmds = [
  "npm ci --prefix dashboard-ui",
  "npm run build --prefix dashboard-ui",
]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
