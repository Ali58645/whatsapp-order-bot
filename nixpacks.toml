# Railway build — install Node deps + build React dashboard into app/static/dashboard

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install", "npm install --prefix dashboard-ui"]

[phases.build]
cmds = ["npm run build --prefix dashboard-ui"]

[variables]
NIXPACKS_NODE_VERSION = "20"
